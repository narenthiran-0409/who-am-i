{
  "name": "who-am-i",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/generate-resume.mjs && vite --host 0.0.0.0 --port 4173",
    "build": "node scripts/generate-resume.mjs && node scripts/validate-data.mjs && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-data.mjs"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "vite": "7.2.2",
    "@vitejs/plugin-react": "5.1.1"
  }
}
